# AgentForge 项目开发规划（v1.3）

> 版本：v1.3（2026-08-03）
> v1.3 变更：M1、M2 已完成并全链路验证；对话模型由 qwen2.5:7b 调整为 qwen3.5:0.8b（低内存 + 默认 think=false）；新增决策 #7/#8（模型目录宿主机挂载、本地模型走 Ollama 原生 API）
> v1.2 变更：最终模型选型落地——Ollama 本地 bge-m3 + qwen2.5:7b，不使用 DeepSeek
> v1.1 变更：纳入 6 项关键决策（见第 2 节）——SSE 提前至 M1、Ollama Embedding 与多会话纳入 M2、Workflow 确定为轻量版、开源协议选 MIT、权限限定为公开/私有
> 依据：《AgentForge 项目初始化设计方案 v0.2》《项目开发任务说明 v0.2》
> 前提：Phase 1 完成；M1（Phase 2 收尾）完成；M2（Phase 3 RAG + 多会话）完成——本地 Ollama 真实模型全链路 E2E 通过

---

## 0. 现状盘点

### 已完成（截至 2026-08-03，均已推送 GitHub 并通过真实模型 E2E）

| 阶段 | 内容 | 状态 |
|---|---|---|
| Phase 1 基础平台 | 后端 8 模块骨架、JWT 认证、Agent CRUD、Vue 登录/注册/Agent 管理、MySQL 5 表 | ✅ 完成 |
| 工程化 | Docker Compose 一键部署、健康检查、全链路 E2E 验证 | ✅ 完成 |
| M1（Phase 2 收尾） | SSE 流式打字机、AI 错误规范化、前端聊天体验、Java 测试基础 | ✅ 完成 |
| M2（Phase 3） | RAG 知识库（bge-m3 + Qdrant 检索 + 来源引用）、文件管理、多会话 | ✅ 完成 |
| 模型环境 | 本地 Ollama：qwen3.5:0.8b（对话，think=false）+ bge-m3（向量）；模型目录挂载 `D:/ollama` | ✅ 完成 |

### 已具备的骨架（M3 直接在此基础上完善）

- RAG：上传 → 落盘 → 解析（pdf/docx/txt/md）→ 切分 → Qdrant 集合隔离 → 检索 → 来源引用，已全链路可用
- 工具：Calculator（AST 安全求值）、Github（API 查询）、统一注册表；**当前为规则触发**（planner 关键字启发式）
- 记忆：Redis `memory.py` 读写已实现但**未接线**，且 key 暂为 `memory:agent:{agentId}`（未区分用户）
- 对话：历史分页、Agent 配置快照透传、错误码映射
- 前端：聊天页（Markdown + 流式渲染）、文件管理页（状态轮询/重试）、Agent 配置页（工具列表）、会话列表/切换

---

## 1. 总体路线图

| 里程碑 | 主题 | 预估工期 | 核心产出 | 状态 |
|---|---|---|---|---|
| M1 | Phase 2 收尾：稳定性、体验与流式输出 | 已完成 | SSE 打字机、错误规范化、Java 测试基础、前端体验 | ✅ |
| M2 | Phase 3：RAG 知识库 + 多会话 | 已完成 | bge-m3 Embedding、端到端知识问答、来源引用、会话列表 | ✅ |
| M3 | Phase 4：Agent 能力增强 | 3-4 周 | LLM 工具循环、Memory 接线、轻量 Workflow v1 | ⏳ 进行中 |
| M4 | Phase 5：工程化与开源发布 | 2-3 周 | CI/CD、测试补全、部署加固、公开/私有、MIT、v1.0.0 | 待开始 |

> 剩余工期按兼职开发（每周约 15-20 小时）估算，M3+M4 约 5-7 周；全职冲刺可压缩至 3-4 周。
> 每阶段均对应求职简历的能力标签：**后端能力 / AI 能力 / 工程能力**，见第 10 节。

---

## 2. 决策记录（已确认，不再返工）

| # | 决策项 | 结论 | 理由 |
|---|---|---|---|
| 1 | 模型选型（Embedding + 对话） | **Ollama 本地双模型：bge-m3（向量）+ qwen3.5:0.8b（对话，默认 `LLM_THINK=false`），不使用 DeepSeek（云端 API 与本地 r1 均不用）** | 全部本地免费、可离线演示、无 Token 费用；本机无独显（CPU 推理、15.2GB 内存），7B 虚拟内存占用过高，0.8B 可稳定运行；bge-m3 多语言检索维度 **1024**；qwen3.5 支持工具调用。代码层保留 OpenAI 兼容抽象，未来可随时切回云端服务 |
| 2 | 多会话 | **做基础版**：新增 `session` 表 + 会话列表/新建/切换/删除，放 M2 | 体验刚需；RAG 上线前定结构避免返工；不做重命名/搜索/分享/归档 |
| 3 | SSE 流式时机 | **提前至 M1** | 打字机效果是演示体验差异最大的功能；独立闭环 2 天内完成，先踩长连接/超时/中断的坑，后续 RAG 与工具循环直接复用 |
| 4 | Workflow 形态 | **轻量 YAML/JSON 流程定义 + LangGraph 编译执行**；可视化拖拽明确不做 | 流程引擎才是技术含量所在；拖拽编辑器是产品级工作量，单人项目必然拖垮工期。后续可补"只读流程图谱"提升观感 |
| 5 | 开源协议 | **MIT** | 最宽松、零合规负担，适合原创个人开源项目；Apache-2.0 的专利/NOTICE 要求对求职展示项目无收益 |
| 6 | 权限增强 | **仅做公开/私有可见性**（`visibility` 字段 + 查询过滤），成员协作砍掉 | 1-2 天提升多用户演示可信度；成员/角色/ACL 后置 v1.1+ |
| 7 | Ollama 模型目录 | 宿主机 bind mount（`D:/ollama`），不存容器层 | 升级容器不重复拉取、模型与容器解耦、节省磁盘 |
| 8 | 本地模型调用协议 | **Ollama 原生 `/api/chat`（think 可控）**；远端模型仍走 OpenAI 兼容 | OpenAI 兼容 `/v1/chat/completions` 不识别 `think` 参数，qwen3.5 会持续输出思考 token 导致响应极慢；原生接口支持 `think=false`，聊天响应提速 10 倍以上 |

---

## 3. M1：Phase 2 收尾（稳定性、体验与流式输出）✅ 已完成

### 交付内容

- SSE 流式输出：AI `/agent/chat/stream` → 后端 `/api/chat/stream` 透传 → 前端打字机渲染（提交 f528f6a / 301472d）
- AI 错误规范化：区分服务不可用 / 模型错误 / 超时，后端错误码映射，前端可读提示
- Java 测试基础：注册/登录/401 拦截/Agent CRUD 集成测试
- 文档同步：`docs/api.md` 补齐 chat/chat-stream 接口与错误码

### 验收结果

- 聊天回答流式呈现，超时/断开有可读提示 ✅
- `mvn test` 通过，核心接口有测试覆盖 ✅

---

## 4. M2：Phase 3 RAG 知识库 + 多会话 ✅ 已完成

### 交付内容

- Ollama 接入：`ollama` 容器 + `ollama-init` 预拉模型；Embedding 走 OpenAI 兼容 `/embeddings`（bge-m3，1024 维），对话走原生 `/api/chat`
- 知识库问答：上传 → 解析 → 切分 → Embedding → Qdrant 集合隔离 → topK 检索 → Prompt 增强 → 回答 + 来源引用；空知识库自动降级普通对话
- 文件管理：状态机（解析中/成功/失败）、重试、删除
- 多会话：`session` 表 + 会话 CRUD API + 前端会话列表/切换（提交 b903aab / b3c3d7d）
- `.env` 已清除 DeepSeek Key；对话模型后调整为 `qwen3.5:0.8b`（提交 e734888 / 1a1f527）

### 验收结果

- 真实模型全链路 E2E 通过（上传 → 检索 → 引用 → 回答）✅
- 多会话切换、历史隔离正常 ✅
- Python 测试 21/21 通过；Docker 8 服务健康 ✅

### 已知遗留（可选清理）

- Qdrant `agent_1` 集合残留一个测试向量（`rag-test.txt`），不影响使用，可一键清理

---

## 5. M3：Phase 4 Agent 能力增强

### 目标

从"单轮问答"进化为"会规划、会用工具、有记忆、能跑流程"的 Agent。

### 5.1 Tool Calling 升级

| 任务 | 说明 | 预估 | 能力标签 |
|---|---|---|---|
| LLM 工具决策 | 规则触发升级为 LLM 依据工具 Schema 自主选择（OpenAI 兼容 `tools` 参数，注意与 `think` 参数并存）；qwen3.5 原生支持工具调用，可直接对接 | 2d | AI |
| 多轮工具循环 | LangGraph 实现 planner → 工具执行 → 结果回填 → LLM 总结（ReAct）；保留规则触发兜底 | 2d | AI |
| 工具 Schema 化 | 注册表补齐 JSON Schema，前端按 Schema 渲染配置表单 | 1.5d | AI+前端 |
| 工具扩展 | 新增 1-2 个工具（如 Web 搜索占位/时间天气），验证可扩展性 | 1d | AI |

### 5.2 Memory 接线

- key 修正为 `memory:agent:{agentId}:user:{userId}`（后端 chat 请求透传 userId），避免多用户串上下文
- 对话后写入最近 N 轮（TTL 24h）；MySQL 历史兜底与跨设备恢复；Redis 不可用自动降级
- 预估 1.5d，能力标签：AI + 后端

### 5.3 Workflow v1（轻量版）

| 任务 | 说明 | 预估 | 能力标签 |
|---|---|---|---|
| 流程定义模型 | `workflow` + `workflow_node` 表、CRUD 接口（YAML/JSON 定义） | 1.5d | 后端 |
| 执行引擎 | 流程定义 → LangGraph 编译执行，节点级日志 | 2d | AI |
| Agent 绑定 | Agent 可选"对话模式 / 工作流模式" | 1d | 后端+前端 |
| （可选）只读可视化 | 前端把流程定义渲染为节点图（不做交互编辑） | 1d | 前端 |

### 验收标准

- Agent 自主决定调用工具并基于结果继续回答；工具失败不影响主链路
- 不同用户使用同一 Agent 互不串记忆
- 简单工作流（如"查仓库 → 算指标 → 生成报告"）可定义并执行，节点日志可查

---

## 6. M4：Phase 5 工程化与开源发布

### 目标

从"能跑的演示项目"升级为"规范的开源项目"。

### 任务分解

| 任务 | 说明 | 预估 | 能力标签 |
|---|---|---|---|
| CI/CD | GitHub Actions：后端 `mvn test`、前端 `npm run build`、Python `pytest`；Docker 镜像构建 | 2d | 工程 |
| 测试补全 | 后端接口测试全覆盖；Python RAG/工具单测；前端冒烟 | 2d | 工程 |
| 部署加固 | HTTPS（nginx + 证书）、日志收集、MySQL/Qdrant 备份策略、资源限额 | 2d | 工程 |
| 多模型配置 | 模型列表/Key 管理页（多 Provider 并存），消除硬编码 | 1.5d | 后端+前端 |
| **公开/私有可见性** | `visibility` 字段 + 列表过滤 + 权限校验（私有仅创建者可见） | 1.5d | 后端 |
| 开源规范 | **MIT LICENSE**、CONTRIBUTING、CODE_OF_CONDUCT、Issue/PR 模板、CHANGELOG | 1d | 工程 |
| 项目宣传 | README 完善（架构图、截图、演示 GIF）、GitHub Topics、Release v1.0.0 | 1d | 工程 |

### 验收标准

- 合并即触发三端自动化验证，红则合并不通过
- 新机器 30 分钟内可从零复现一键部署
- 仓库具备完整开源协作要素，可对外展示

---

## 7. 时间线与里程碑检查点

```text
第 1-4.5 周    M1 + M2 ✅ 已完成（Phase 2 收尾 + Phase 3 RAG/多会话）
第 5-8.5 周    M3 Phase 4 Agent 能力（LLM 工具循环 → Memory → Workflow v1）
第 9-11 周     M4 Phase 5 工程化与发布（CI/CD → 加固 → MIT 开源规范 → v1.0.0）
```

每个里程碑结束做一次演示验收（录屏 + 更新 README 截图），沉淀求职材料。

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| Ollama 模型拉取网络问题 | RAG 无法落地 | compose 内预拉模型；失败换镜像源或临时换 nomic-embed-text（配置级切换） |
| bge-m3 维度与 Qdrant 集合不一致 | 检索报错 | 维度常量集中配置 + 集合重建脚本（已固化 1024，M2 已验证） |
| 0.8B 模型回答质量有限（偶尔答非所问） | 演示效果打折 | 联调可接受；正式演示前按内存余量升级 qwen2.5:1.5b/3b（仅改 `.env` 配置） |
| CPU 推理慢（长 RAG 上下文 25-115s） | 等待感强 | `LLM_THINK=false` 已默认；SSE 流式缓解感知；可裁剪 RAG 检索条数/上下文长度 |
| 7B 模型虚拟内存占用过高 | 本机卡顿 | 已换 0.8B；模型目录绑定宿主机 `D:/ollama`，模型与容器解耦 |
| LLM 工具调用不稳定 | 工具循环体验差 | 保留规则触发兜底；工具 Schema 严格校验 |
| Workflow 复杂度膨胀 | 工期失控 | 轻量 YAML 流程限定节点类型；可视化明确后置 |
| 时间冲突（求职季） | 进度拖延 | 里程碑可裁剪：Tool 循环与 Memory 优先，Workflow 可后置 |
| 密钥/数据安全 | 泄露风险 | `.env` 隔离；CI 密钥用 Secrets；演示数据脱敏 |

---

## 9. 后置功能池（本轮明确不做）

- Workflow 可视化拖拽编辑器（只读图谱可加）
- 成员协作、角色权限、会话分享/搜索/归档
- 多租户与插件市场
- 移动端适配

---

## 10. 求职价值对照

| 能力维度 | 本项目对应内容 | 简历表达 |
|---|---|---|
| 后端能力 | 8 模块 Maven 架构、JWT/Spring Security、MyBatis Plus、Redis、多会话 | 企业级 Spring Boot 多模块架构设计与实现 |
| AI 能力 | LangGraph Agent、RAG（Qdrant + Ollama）、Tool Calling、Memory、SSE 流式 | 完整落地 LLM Agent + RAG 应用，支持多模型与流式输出 |
| 工程能力 | Docker Compose、CI/CD、自动化测试、MIT 开源规范 | 全链路 DevOps 交付，一键部署 + 自动化质量门禁 |

---

## 11. 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-03 | 初始规划（M1-M4 四阶段路线） |
| v1.1 | 2026-08-03 | 固化 6 项决策：SSE 提前至 M1、Ollama+多会话入 M2、Workflow 轻量化、MIT、公开/私有权限；补充后置功能池与风险条目 |
| v1.2 | 2026-08-03 | 最终模型选型落地：Ollama 本地 bge-m3 + qwen2.5:7b，不使用 DeepSeek（云端 API 与本地 r1 均不用）；同步 M1 验收、M2 前置与风险表 |
| v1.3 | 2026-08-03 | M1/M2 标记完成并补充验收结果；对话模型调整为 qwen3.5:0.8b（低内存 + think=false）；新增决策 #7（模型目录宿主机挂载）与 #8（本地模型原生 API）；更新路线图、时间线、风险表 |
